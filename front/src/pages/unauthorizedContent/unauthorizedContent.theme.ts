import { makeStyles } from '@mui/styles';

const useStylesUnauthorizedContent = makeStyles(() => ({
  unauthorizedContent: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100vw',
    height: '100vh',
  },
}));

export default useStylesUnauthorizedContent;
